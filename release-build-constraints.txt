# Exact build-tool pins shared by ci.yml (artifact job) and publish.yml.
# Bump both by editing this file; the publish digest guard proves the result
# still reproduces the reviewed bytes. These are the versions the maintainer's
# build venv (F:/Projects/.venvs/graphite-build) held when 1.0.0 was cut.
build==1.5.0
hatchling==1.32.0
